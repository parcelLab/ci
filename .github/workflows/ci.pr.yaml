name: PR
on:
  pull_request:
    types:
      - opened
      - edited
      - ready_for_review
      - reopened
      - synchronize
jobs:
  pr:
    uses: parcelLab/.github/.github/workflows/pr.yaml@v1
    secrets:
      repoAccessToken: ${{ secrets.GITHUB_TOKEN }}
